class Validator {
    isValidUser(user) {
        return true;
    }
    isValidPoints(points) {
        return true;
    }
    lengthValidation(min, max, value) {
        return true;
    }

    save() {}
    static find() {}
    static findOne() {}
};

module.exports = Validator;