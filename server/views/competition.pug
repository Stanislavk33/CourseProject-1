extends _layout

block content
    .wrapper
        .header.header-filter(style="background-image: url('/static/imgs/nature-wallpapers26.jpg');")
        .main.main-raised(style="margin-top: -500px;")
            .profile-content
                .container
                    div.competition-info(id="info" data-id=result._id)
                    .row
                        .col-md-4
                            .competition-map(id="map")
                            img.img-rounded.img-responsive(src='http://lorempixel.com/250/140/people',width="300px", height="auto;", alt='Lorem ipsum')
                            block like
                        .col-md-6
                            .col-md-4
                            if result.passed
                                span.label.label-warning #{result.passed}
                            else
                                span.label.label-success.active-comp Active
                            .col-md-4
                            block joining 
                            h2.title Category name: #{result.name}
                            h2 Place: #{result.place}
                            h3 Organized by:
                                a(href="users/" +result.organizator)  #{result.organizator}
                            h3 Category: #{result.category}
                            h3 Level: #{result.level}
                            h3 Points: #{result.points}
                            span #{result.startDate}
                            span #{result.endDate}
                            span.hidden(id='latitude') #{result.location.latitude}
                            span.hidden(id='longitude') #{result.location.longitude}
                        .col-md-8
                            h2.title Joined users
                            table.table.users-table(id="joined-users")
                                thead
                                    tr
                                        th User
                                        th Add points
                                    each user in result.joinedUsers
                                        tr 
                                            td
                                                a.title(href="/users/" + user.username) #{user.username}
                                            
                                            if result.isOrgan
                                            td
                                                button.btn.btn-info.addPoints(data-id=user.username +" "+ result.points data-category=result.category) addPoints
block scripts

    script(src="/static/bower_components/jquery/dist/jquery.min.js")
    script(src="/static/bower_components/toastr/toastr.js")
    script(src="/static/js/add-points.js")
    script(src="/static/js/competition.js")
    script(src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC0PmHg9An9AdDbib9-jYefjSKSnapzFLQ&callback=initMap")


    
                

             
                    